export enum LabelVariant {
    default,
    primary,
    secondary,
    accent,
    success,
    attention,
    severe,
    danger,
    done,
    sponsors,
}

export enum LabelSize {
    small,
    large,
}
