/// **FilteredActionList** demos — **`lines`** are filled and narrowed by **Node** / **Rust** (prefix match).

import { ActionListLine } from "../../../primer-slint/primer.slint";

export global GalleryFilteredActionListDefault {
    in-out property <[ActionListLine]> lines: [];
    callback filter-changed(string);
}

export global GalleryFilteredActionListLong {
    in-out property <[ActionListLine]> lines: [];
    callback filter-changed(string);
}
