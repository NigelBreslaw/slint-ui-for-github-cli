/// **ActionList** menu multi-select — **Node** `wireIndexMultiSelect` (Copy link / Quote reply / Edit comment).
export global GalleryActionListMenuMultiSelect {
    in-out property <[bool]> row-checked: [false, false, false];
    in-out property <string> last-activated-label: "";
    in-out property <string> selection-summary: "";
    callback row-activated(int);
}
