/// **FilteredActionList** multi-select demo — toggled from **Node** / **Rust** by label (upstream **SelectPanel** `MultiSelect` initial `items.slice(1, 3)`).

import { ActionListLine } from "../../../primer-slint/primer.slint";

export global GalleryFilteredActionListMulti {
    in-out property <[ActionListLine]> lines: [];
    in-out property <[bool]> multi-selected: [];
    callback filter-changed(string);
    callback item-activated(int);
}
