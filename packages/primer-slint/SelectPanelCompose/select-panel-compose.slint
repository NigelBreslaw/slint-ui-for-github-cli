
import { SelectPanelTokens } from "../tokens.slint";

export component SelectPanelCompose {
    horizontal-stretch: 1;
    width: 100%;

    VerticalLayout {
        spacing: SelectPanelTokens.wrapper-stack-gap;
        width: 100%;
        alignment: start;

        @children
    }
}
