/// Primer CounterLabel **`scheme`**: `primary` = neutral emphasis fill, `secondary` = muted fill (default **`background`** / **`foreground`** in **`counter-label.slint`**).
export enum CounterLabelVariant {
    primary,
    secondary,
}
