# DirectionProvider

**Status:** Stub (no Slint export yet).

**Upstream:** `packages/react/src/direction-provider/`

RTL/LTR via app-level `BaseUiDirection` global (Phase 2), not a provider component.
