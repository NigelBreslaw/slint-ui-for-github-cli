// Public barrel — Phase 1 exports foundation tokens only; widget namespaces added in Phase 2+.
export {
    BaseUiColors,
    BaseUiFocusTokens,
    BaseUiLayoutTokens,
    BaseUiOpacityTokens,
} from "./tokens.slint";
