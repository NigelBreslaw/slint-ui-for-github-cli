/// Updated by the Node bridge when the user selects a stub leaf.
export global GalleryStubContent {
    in-out property <string> title: "";
    in-out property <string> upstream-path: "";
    in-out property <int> target-phase: 2;
    in-out property <string> notes: "";
}
