/// Neutral headless tokens for base-ui-gallery (temporary until packages/base-ui-slint exists).
export global BaseUiGalleryColors {
    out property <brush> bg-default: #f4f4f5;
    out property <brush> bg-subtle: #fafafa;
    out property <brush> fg-default: #18181b;
    out property <brush> fg-muted: #71717a;
    out property <brush> border-default: #d4d4d8;
    out property <brush> header-bg: #ffffff;
}

export global BaseUiGalleryLayout {
    out property <length> sidebar-width: 240px;
    out property <length> header-height: 64px;
}
